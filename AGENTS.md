# PokeWar Agent Constitution

本文件是 Hermes + DeepSeek V4 Pro 在本仓库中的最高级开发约束。任何 Agent 都不得以“优化”“顺手重构”“更现代”为理由越权。

## 1. 真源优先级

1. `references/RULES.md`：玩法唯一真源。没有用户明确批准，不得改变规则语义。
2. `docs/ADR/*.md`：已批准的架构决定。
3. `references/POKEWAR_REFACTOR_GUIDE.md`：失败经验与架构约束，不是玩法规则。
4. `references/design.md` 与 `docs/STYLE_SYSTEM.md`：视觉规范；冲突时以已批准 ADR 为准。
5. 代码与测试：必须服从上述文档，而不是反向定义规则。

## 2. Agent 绝对禁止事项

- 不得修改 `references/`。
- 不得同时修改超过一个业务边界，除非任务明确列出所有边界。
- 不得在 `packages/domain` 中导入 Node、DOM、数据库、网络、日志或 UI 依赖。
- 不得绕过共享协议直接发送未校验 JSON。
- 不得使用全局可变单例保存游戏真状态。
- 不得在模块顶层访问可能未初始化的浏览器全局对象。
- 不得添加 CDN 运行时依赖。
- 不得使用 `sort(() => Math.random() - 0.5)` 洗牌。
- 不得通过 `filter: invert()` 实现暗色模式。
- 不得删除失败测试、降低断言强度或用 `any` 逃避类型检查。
- 不得执行 `rm -rf`、覆盖生产数据库、强推主分支或打印密钥。

## 3. 每个任务必须提供

- 任务 ID、目标、允许路径、禁止路径、非目标。
- 可验证验收标准。
- 最小测试集合。
- 变更摘要、风险、回滚方式。
- 无法确认的事实必须标记 `NEEDS_USER_DECISION`，不能猜。

## 4. 变更预算

默认单任务预算：最多 8 个文件、400 行净新增/修改。超过预算必须拆分任务；纯格式化、生成锁文件和快照不计入，但必须单独提交。

## 5. 停止条件

发现以下情况立即停止编码并只输出诊断：规则冲突、数据迁移不可逆、需要新增生产依赖、接口破坏性变化、测试基线未知、任务要求超出允许路径。

## 6. 完成定义

`pnpm verify` 全绿；没有新增架构违规；文档同步；没有隐藏 TODO；输出符合 `.hermes/prompts/OUTPUT_CONTRACT.md`。
